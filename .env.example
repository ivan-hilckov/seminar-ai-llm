# Set these in Dokploy → Environment (never commit real values).
# The only env this service needs — Traefik routing, no secrets:

SERVICE_DOMAIN=llm.heliotik.xyz   # the host this site answers on
SERVICE_NAME=llm                  # unique Traefik router id on the machine (letters/digits/-, no dots/spaces)
