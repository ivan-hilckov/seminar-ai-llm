# Семинар «AI и LLM для исследователей» — обзор проекта

> Этот файл — краткий, но полный паспорт проекта для внешнего ассистента (Claude.ai), который не работал в репозитории. Он позволяет за один прочтённый файл понять, что это, чем сделано, как устроено и в каком оно состоянии.

**Дата семинара:** 13 мая 2026
**Место:** ФБУ ВНИИЛМ
**Ведущие:** Иван Хильков, Андрей Гиценко
**Аудитория:** научные сотрудники, новички в AI/LLM
**Длительность:** 5 ч 15 мин (3 секции по 90 мин + перерывы)

**Главная идея:** объясняем устройство LLM через простые аналогии (Т9 → Smart Compose → GPT → ChatGPT → думающие модели). Понимание устройства само снимает преждевременные страхи — без прямого обращения к теме страхов.

**Главный тезис Секции 1 (слайд 28 ★, keystone):** «Это не интеллект. Это статистика.»

**Источник истины для содержания слайдов:** `stuff/md/section-1-map.md` для Секции 1, `stuff/md/Section_2_slides_draft.md` для Секции 2. Реестр в коде синхронизируется с этими файлами; при расхождениях верят документам.

> **Важно про нумерацию.** Из Секции 1 убраны 8 проходных E-моментов, и она перенумерована подряд → **06–48** (keystone стал слайдом 28). Сквозной нумерации 01–N без пропусков больше нет: между секциями есть разрывы (05 → 06 → … → 48 → 63 → … → 87). Навигация и sidebar идут по индексу в `allSlides`, а не по арифметике номеров.

---

## 1. Что это технически

Веб-приложение для разработки и показа слайдов:

- **Стек:** Vite 5 + React 18 + React Router v6 + react-markdown (remark-gfm), JavaScript (.jsx), без TypeScript.
- **Менеджер пакетов:** pnpm 10 через mise (Node 25).
- **Слайды:** React-компоненты. Каждый слайд рендерится в фикс-канваса 1920×1080 с динамическим масштабированием под viewport через `useFit`.
- **Заметки ведущего:** Markdown-файлы, рендерятся через `react-markdown` со стилизованным prose.
- **Печать в PDF:** `Cmd+P` → Save as PDF → Landscape; в CSS зашиты `@page { size: 1920px 1080px }` и `@media print` правила.
- **Dev:** `pnpm dev` на `http://localhost:5190/`.
- **Prod:** `pnpm build` → статика в `dist/`.

---

## 2. Структура каталогов

```
seminar-ai-llm/
├── docs/                          # документация (этот файл — здесь)
├── public/
│   ├── templates/                 # статические HTML-шаблоны A/B/C/D/E (исторический референс)
│   ├── seminar-ai-llm.pdf         # готовый PDF всего семинара (перегенерируется через /print)
│   └── stuff → ../stuff           # symlink на референсы (доступны через /stuff/...)
├── src/
│   ├── main.jsx                   # React entry + BrowserRouter
│   ├── App.jsx                    # роуты
│   ├── routes/
│   │   ├── Index.jsx              # /            — главная навигация
│   │   ├── Opening.jsx            # /opening     — карта открытия (5 слайдов)
│   │   ├── Section1.jsx           # /section/1   — карта Секции 1 (43 слайда)
│   │   ├── Section2.jsx           # /section/2   — карта Секции 2 (23 слайда)
│   │   ├── Print.jsx              # /print       — все слайды подряд для печати в PDF
│   │   ├── Slide.jsx              # /slide/:id   — рендер слайда + sidebar + navbar
│   │   ├── Notes.jsx              # /notes/:id   — speaker notes в стилизованном prose
│   │   └── NotFound.jsx
│   ├── components/
│   │   ├── Stage.jsx              # .stage + .slide контейнер с useFit
│   │   ├── Meta.jsx               # верхняя плашка «XX [тип]»
│   │   ├── Foot.jsx               # «AI & LLM · ФБУ ВНИИЛМ»
│   │   ├── MarkdownView.jsx       # react-markdown wrapper
│   │   ├── SlideNav.jsx           # плавающий бар: ‹ X/N › ↻
│   │   ├── SlideSidebar.jsx       # левый сайдбар с миниатюрами всех слайдов
│   │   └── ThumbnailContext.jsx   # сигнал «мы внутри миниатюры»
│   ├── hooks/
│   │   └── useFit.js              # адаптивное масштабирование .slide
│   ├── slides/
│   │   ├── registry.js            # findSlide / sectionOf / allSlides / partOf (71 запись)
│   │   ├── opening/
│   │   │   ├── index.js           # реестр открытия (5 записей)
│   │   │   ├── opening.css        # стили s01/s02/s03
│   │   │   ├── Slide01–05.jsx     # титул, карта дня, цели С1/С2/С3
│   │   │   └── GoalsSlide.jsx     # общий компонент для 03/04/05
│   │   ├── section-1/
│   │   │   ├── index.js           # реестр Секции 1 (43 записи, все done)
│   │   │   ├── ASlide.jsx         # универсальный A-слайд (section divider)
│   │   │   ├── ESlide.jsx         # универсальный E-слайд (momentum/number/live)
│   │   │   ├── StubSlide.jsx      # type-aware заглушка для черновиков (всех секций)
│   │   │   ├── Slide06.jsx        # A · I · Откуда взялись современные модели
│   │   │   ├── Slide07.jsx        # C · Nokia с Т9 (SVG)
│   │   │   ├── Slide08.jsx        # C · схема Т9
│   │   │   ├── Slide09.jsx + .css # D · Т9 vs LLM
│   │   │   ├── Slide10.jsx        # C · Smart Compose в Gmail (SVG-окно письма)
│   │   │   ├── Slide11.jsx        # C · Автодополнение в поиске (SVG-Яндекс)
│   │   │   ├── …                  # 06–48, см. раздел 7.2
│   │   │   ├── Slide28.jsx        # E ★ · Это не интеллект. Это статистика. (keystone)
│   │   │   ├── Slide29.jsx        # A · II · Как это работает внутри
│   │   │   ├── Slide37.jsx        # A · III · Особенности в работе
│   │   │   ├── a-slides.css       # стили A (огромная римская цифра italic)
│   │   │   ├── e-slides.css       # стили E (momentum / number / live)
│   │   │   └── stub-slide.css     # стили заглушек
│   │   └── section-2/
│   │       ├── index.js           # реестр Секции 2 (23 записи, Части IV–V)
│   │       ├── part-v.css         # общие стили слайдов Части V
│   │       └── Slide63–87.jsx     # реализованные слайды (с пропусками 65, 66)
│   ├── notes/
│   │   ├── opening/
│   │   │   └── slide-01.md … slide-03.md
│   │   └── section-1/
│   │       ├── _template.md
│   │       └── slide-07.md … slide-48.md   # заметки для всех слайдов С1, кроме заставки 06
│   └── styles/
│       ├── global.css             # палитра + типографика + сбросы
│       ├── slide.css              # .stage, .slide, .meta, .foot, .visual, .right, @page
│       └── prose.css              # стили markdown viewer
├── stuff/                         # референсные материалы
│   ├── md/                        # программа, бриф, drafts, speaker notes, КАРТА секции 1
│   ├── pdf/                       # PDF-эталоны
│   ├── img/                       # скриншоты
│   ├── Concept v1.html            # концепт-шаблоны типов A/B/C/D/E
│   ├── Pair 1 - Opening.html      # исходник открытия (5 слайдов)
│   ├── Section 1 Skeleton.html    # старый черновик (нумерация устарела вдвойне)
│   ├── Section 1 E-slides.html    # исходные E-слайды (большинство убраны из деки)
│   └── ...                        # см. раздел 12
├── .mise.toml                     # node + pnpm версии
├── package.json
├── vite.config.js                 # dev-сервер на порту 5190 (strictPort)
├── index.html                     # Vite entry, загружает шрифты
├── README.md
└── CLAUDE.md                      # паспорт для Claude Code
```

---

## 3. Маршруты приложения

| Path | Что рендерит |
|---|---|
| `/` | Главная: ссылки на карты открытия / С1 / С2, список готовых слайдов **с разбивкой по секциям**, ссылки на референсы |
| `/opening` | Карта открытия — 5 миниатюр карточек |
| `/section/1` | Карта Секции 1 — сетка из 43 карточек, сгруппированных по 3 частям + закрытие |
| `/section/2` | Карта Секции 2 — сетка из 23 карточек, сгруппированных по Частям IV / V |
| `/print` | Все слайды подряд для печати в один PDF |
| `/slide/:id` | Один слайд + левый sidebar с миниатюрами всех слайдов + плавающий навбар |
| `/notes/:id` | Speaker notes слайда в стилизованном prose (или сообщение «нет заметок») |

`:id` — строковый номер слайда («07», «28», «70», …). Существующие id заданы реестром; запросы к несуществующим (например, удалённым старым номерам) уходят в NotFound.

Все маршруты — клиентский SPA. На dev истории fallback'ом обслуживает Vite, в проде нужно настроить fallback на `index.html`.

---

## 4. Архитектура слайд-движка

### 4.1 Stage + useFit

`<Stage label="07" className="...">{children}</Stage>` рендерит:

```html
<div class="stage">                  /* fixed, top:0 right:0 bottom:0 left:240px (под sidebar) */
  <article class="slide ...">         /* 1920×1080, transform: scale(...) */
    {children}
  </article>
</div>
```

`useFit(enabled)` считает `min(viewportW/1920, viewportH/1080)` (с учётом сайдбара 240px) и применяет `transform: scale(s)` к `.slide`. При ресайзе пересчитывает.

### 4.2 ThumbnailContext (миниатюры)

В `SlideSidebar` тот же `<SlideComponent />` рендерится внутри `<ThumbnailProvider value={true}>`. Внутри:
- `Stage` пропускает обёртку `.stage`, рендерит только `.slide`.
- `useFit` ничего не делает (enabled=false).
- Родительский `.thumb-row__scale` задаёт `transform: scale(0.10416)` (200/1920) → миниатюра 200×112.

Так миниатюры — это **настоящий рендер слайда в маленьком масштабе**, не отдельный код. Работает и для готовых компонентов, и для `StubSlide`-заглушек.

### 4.3 SlideNav

Плавающий бар внизу страницы `/slide/:id`:
- `‹` prev / `X / N` / `›` next — навигация по индексу в `allSlides` (N = всего слайдов)
- `↻` — назад на карту: `/opening`, `/section/1`, `/section/2` (по `sectionOf(id)`)
- Клавиатура: `←`/`PageUp`, `→`/`PageDown`/`Space`, `Esc` → карта.

### 4.4 Print (PDF)

В `slide.css`:

```css
@page { size: 1920px 1080px; margin: 0; }

@media print {
  html, body { width: 1920px !important; height: 1080px !important; ... }
  .stage { position: static; left: 0; ... }
  .slide { transform: none !important; page-break-after: always; }
  .slide-sidebar, .slide-nav { display: none !important; }
}
```

Печать `/print` (все слайды) или `/slide/:id` (один) → PDF 1920×1080, sidebar и navbar спрятаны.

---

## 5. Типы слайдов (5 шаблонов)

| Тип | Назначение | Композиция |
|---|---|---|
| **A** | Section divider | Огромная римская цифра italic accent (`I/II/III/IV/V/VI`, 780px, IBM Plex Serif italic), тонкая линия сверху, заголовок справа внизу, мета слева внизу |
| **B** | Тезис | Один тезис по центру (44–56px), подблок сверху, опциональный «anchor →» снизу |
| **C** | Визуал + текст | 62% графика слева (SVG или скриншот) + 38% текст справа (sub / title / cap), центрирован по вертикали относительно визуала (`.right { top: 140px; height: 800px; justify-content: center }`) |
| **D** | Сравнение А/Б | Заголовок сверху, две колонки с центральной 1px-линией, опциональный summary внизу |
| **E** | Moment (3 подтипа) | `momentum` — одна фраза по центру 96px; `number` — крупное число 120px + caption; `live` — заглушка под живую демонстрацию |

**Универсальные компоненты** (`ESlide`, `ASlide`) принимают пропы и рендерят соответствующий variant. Конкретные `SlideXX.jsx` — тонкие обёртки. Так слайды одного типа разделяют разметку и стили.

> Тип **E** остаётся в системе, но из Секции 1 все E-моменты убраны, кроме keystone. Единственный E-слайд С1 — **28** «Это не интеллект. Это статистика.» (momentum + accent).

`StubSlide` сам определяет римскую цифру и название части по id (`getPartInfo`): покрывает все секции, включая Секцию 2 (Часть IV / V / VI).

---

## 6. Реестр слайдов — контракт

`src/slides/section-1/index.js` (и аналогично `opening/`, `section-2/`):

```js
const ready = (id, type, title, subblock, component, extra = {}) => ({
  id,            // "07"
  type,          // "A" | "B" | "C" | "D" | "E"
  title,         // короткий лейбл для карты (не для самого слайда)
  subblock,      // "1.1 Т9 на Nokia"
  component,     // React-компонент слайда
  done: true,    // ★ готов или нет — отдельно от наличия компонента
  hasNotes: extra.hasNotes !== false,   // есть ли .md в src/notes/section-1/
  keystone: !!extra.keystone,           // главный тезис (28) — акцентная обводка
});

const draft = (id, type, title, subblock, extra = {}) => ({
  id, type, title, subblock,
  component: null,    // → отрендерится через StubSlide
  done: false,        // ★ карта показывает как черновик
  hasNotes: false,
  keystone: !!extra.keystone,
});
```

**Важное разделение:**
- `component` — есть ли React-компонент для рендера (если нет → StubSlide).
- `done` — считаем ли слайд готовым (карта читает это поле; черновики в карте — `is-draft`).

На текущий момент все зарегистрированные слайды Секции 1 (43) и Секции 2 (23) — `ready`. Хелпер `draft` сохранён для будущих секций.

`src/slides/registry.js` объединяет три реестра:

```js
export const allSlides = [...openingSlides, ...section1Slides, ...section2Slides];   // 5 + 43 + 23 = 71
export function findSlide(id);    // null если не существует
export function sectionOf(id);    // 'opening' | 'section-1' | 'section-2' | null
```

---

## 7. Состояние слайдов

### 7.1 Открытие (5 слайдов · 01–05)

Все 5 — **done**, реализованы как React-компоненты.

| id | Тип | Заголовок | Notes |
|---|---|---|---|
| 01 | E | Титул — «AI и LLM для исследователей» | ✓ |
| 02 | C | Карта дня (3 секции, current = Секция 1) | ✓ |
| 03 | B | Цели Секции 1 | ✓ |
| 04 | B | Цели Секции 2 | — |
| 05 | B | Цели Секции 3 | — |

### 7.2 Секция 1 (43 слайда · 06–48) — все done

Все слайды реализованы как React-компоненты. Speaker notes есть для всех, кроме заставки 06 (A-дивайдеры 29 и 37 заметки имеют). Keystone — слайд **28**.

| id | Тип | Заголовок | Подблок | Notes | Спец. |
|---|---|---|---|---|---|
| 06 | A | Откуда взялись современные модели | 1.0 Заставка | — | I |
| 07 | C | Помните это? | 1.1 Т9 на Nokia | ✓ | SVG Nokia |
| 08 | C | Как работал Т9 | 1.1 Т9 на Nokia | ✓ | SVG схема |
| 09 | D | Т9 vs LLM | 1.1 Т9 на Nokia | ✓ | сравнение |
| 10 | C | Smart Compose в Gmail | 1.2 Автозаполнение | ✓ | SVG окно письма |
| 11 | C | Автодополнение в поиске | 1.2 Автозаполнение | ✓ | SVG Яндекс-строка |
| 12 | C | 2017: Transformer · Attention Is All You Need | 1.3 От Transformer до GPT-3 | ✓ | обложка/документ |
| 13 | C | Attention: каждое слово учитывает контекст | 1.3 От Transformer до GPT-3 | ✓ | график/схема |
| 14 | D | 2018: GPT-1 · последовательно vs всё сразу | 1.3 От Transformer до GPT-3 | ✓ | сравнение |
| 15 | C | 2019: GPT-2 · модель прочитала интернет | 1.3 От Transformer до GPT-3 | ✓ | график |
| 16 | C | Линия GPT | 1.3 От Transformer до GPT-3 | ✓ | |
| 17 | C | Emergent abilities | 1.3 От Transformer до GPT-3 | ✓ | |
| 18 | B | Попугай, прочитавший интернет | 1.3 От Transformer до GPT-3 | ✓ | метафора |
| 19 | C | ChatGPT, 30 ноября 2022 | 1.4 ChatGPT 2022 | ✓ | |
| 20 | C | RLHF · обучение с обратной связью от человека | 1.4 ChatGPT 2022 | ✓ | SVG схема |
| 21 | C | За сколько сервисы добирались до 100 миллионов | 1.4 ChatGPT 2022 | ✓ | график |
| 22 | B | 2024–2025: модели, которые думают | 1.5 Думающие модели | ✓ | |
| 23 | C | Chain of Thought | 1.5 Думающие модели | ✓ | |
| 24 | D | Обычная модель vs думающая | 1.5 Думающие модели | ✓ | сравнение |
| 25 | D | Что меняется | 1.5 Думающие модели | ✓ | сравнение |
| 26 | D | У думающих моделей выше галлюцинации | 1.5 Думающие модели | ✓ | сравнение |
| 27 | C | От слова к рассуждению | 1.6 Главный вывод | ✓ | |
| **28** | E | **Это не интеллект. Это статистика.** | 1.6 Главный вывод | ✓ | **keystone · accent** |
| 29 | A | Как это работает внутри | 2.0 Заставка | ✓ | II |
| 30 | B | Откуда модель знает, что «врач» и «доктор» близки? | 2.1 Векторы смыслов | ✓ | |
| 31 | C | Пространство смыслов | 2.1 Векторы смыслов | ✓ | |
| 32 | B | Близость = смысл | 2.1 Векторы смыслов | ✓ | |
| 33 | C | Что такое контекст | 2.2 Контекст и память | ✓ | |
| 34 | D | Новый чат — новый лист | 2.2 Контекст и память | ✓ | сравнение |
| 35 | C | Память (Memory) — отдельная функция | 2.2 Контекст и память | ✓ | |
| 36 | B | Модель обучают один раз | 2.3 Обучение и заморозка | ✓ | |
| 37 | A | Особенности в работе | 3.0 Заставка | ✓ | III |
| 38 | D | Облачно vs локально | 3.1 Данные и приватность | ✓ | сравнение |
| 39 | C | Как отключить обучение на ваших данных | 3.1 Данные и приватность | ✓ | SVG mock |
| 40 | B | Когда Память полезна, когда мешает | 3.2 Память и контекст в работе | ✓ | |
| 41 | C | Поиск — отдельная функция | 3.3 Доступ к интернету | ✓ | |
| 42 | B | Не знает фактов — знает статистику текстов | 3.4 Точность и галлюцинации | ✓ | |
| 43 | D | Как выглядит галлюцинация | 3.4 Точность и галлюцинации | ✓ | сравнение |
| 44 | D | Где проверять обязательно | 3.4 Точность и галлюцинации | ✓ | сравнение |
| 45 | B | AI берёт на себя задачи, не роль | 3.5 Границы применения | ✓ | |
| 46 | B | Она понимает буквально — не интуитивно | 3.5 Границы применения | ✓ | |
| 47 | C | Что мы прошли | Закрытие | ✓ | |
| 48 | B | Главные тезисы | Закрытие | ✓ | |

### 7.3 Разбивка Секции 1 по частям

| Часть | id-диапазон | N слайдов | Время | Тема |
|---|---|---|---|---|
| **I** | 06–28 | 23 | ≈ 45 мин | Откуда взялись современные модели |
| **II** | 29–36 | 8 | ≈ 25 мин | Что происходит при ответе |
| **III** | 37–46 | 10 | ≈ 16 мин | Важные особенности |
| **Закрытие** | 47–48 | 2 | ≈ 2 мин | Что прошли, главные тезисы |

Подблоки внутри частей:

- **1.0** Заставка — 06
- **1.1** Т9 на Nokia — 07–09
- **1.2** Автозаполнение — 10–11
- **1.3** От Transformer до GPT-3 — 12–18
- **1.4** ChatGPT 2022 — 19–21
- **1.5** Думающие модели — 22–26
- **1.6** Главный вывод — 27–28 (28 ★ keystone)
- **2.0** Заставка — 29
- **2.1** Векторы смыслов — 30–32
- **2.2** Контекст и память — 33–35
- **2.3** Обучение и заморозка — 36
- **3.0** Заставка — 37
- **3.1** Данные и приватность — 38–39
- **3.2** Память и контекст в работе — 40
- **3.3** Доступ к интернету — 41
- **3.4** Точность и галлюцинации — 42–44
- **3.5** Границы применения — 45–46
- **Закрытие** — 47–48

### 7.4 Секция 2 (23 слайда · 63–87) — все done

Источник: `stuff/md/Section_2_slides_draft.md`. Тема — «Методика работы с языковыми моделями». Реализованы Части IV и V; **Часть VI (88–102) пока скрыта из реестра** и не отображается. Слайды 65 и 66 удалены (дублировали тему «Думающие модели» из С1) — отсюда пропуск в нумерации 64 → 67.

| Часть | id-диапазон | N | Тема |
|---|---|---|---|
| **IV** | 63–69 | 5 | Карта инструментов (заставка, сервисы LLM, тарифы и API, облако vs локально) |
| **V** | 70–87 | 18 | Как формулировать запросы (формула промта, плохо/хорошо ×3, итерация на редактуре, 4 шаблона) |

Подблоки:

- **4.0** Заставка — 63
- **4.1** Сервисы LLM — 64
- **4.3** Тарифы и API — 67–68
- **4.4** Облако vs локально — 69
- **5.0** Заставка — 70
- **5.1** Формула промта — 71–72
- **5.2** Плохой vs хороший промт — 73–78 (Литобзор / Письмо / Код, по паре)
- **5.3** Итерация на редактуре — 79–82
- **5.4** Четыре шаблона — 83–87 (Редактура / Литобзор / Разбор данных / Письма и рецензии)

Speaker notes для слайдов С2 пока нет (`hasNotes: false`). Keystone-слайдов в С2 нет (главный тезис принадлежит С1).

### 7.5 Секция 3

Программа есть (см. `stuff/md/Программа_семинара_AI_LLM_v3_1.md`), реализации и реестра в коде пока нет.

---

## 8. Карты секций

**`/section/1`** и **`/section/2`** — общие стили (`Section1.css`), общий React-шаблон (карточка / part-grid / легенда). Различаются только данными из реестра и параметром `PART_ORDER`.

Header: эйбров `Секция X · N слайдов · 90 минут`, крупный H1, легенда типов A/B/C/D/E. Блоки с заголовком и statsbar справа (`id-range · N слайдов · ≈ X мин`).

Карточка: номер слева вверху + цветной чип типа справа вверху + короткий title внизу + «✎» в углу если есть notes. Тинт фона = тип (черновики без тинта — видно прогресс). Keystone — accent-обводка.

Клик по карточке → `/slide/:id`. Клик по «✎» → `/notes/:id` (stopPropagation).

**`/opening`** — мини-grid 5 карточек в одном блоке.

---

## 9. Speaker notes

`.md` файлы в `src/notes/`. Vite собирает их через `import.meta.glob('*.md', { query: '?raw', eager: true })` — никакого рантайм-фетча.

**Структура заметки** (см. `_template.md`):

1. Заголовок слайда + блок-метаданные (тип, хронометраж, ведущий, цель)
2. Технический фон (для уверенности ведущего)
3. Концептуальный мост (как связан с другими слайдами)
4. Полу-сценарий выступления (прямая речь в blockquotes)
5. На что обращать внимание (темп, паузы, реакция зала)
6. Возможные вопросы (Q&A)
7. Связки со слайдами

**Стилизация (`src/styles/prose.css`):**
- IBM Plex Sans, 17px / 1.6 line-height
- Первый blockquote (метаданные) — выделен серым фоном `--hi`
- Остальные blockquotes — левый border accent (`--accent`)
- Таблицы с моно-заголовками, светлой подложкой
- Inline code и pre с моно-фоном

**Покрытие заметками:** открытие 01–03; Секция 1 — все слайды 07–48 (кроме заставки 06). Секция 2 — пока без заметок.

> Заметки С1 содержат рукописные перекрёстные ссылки на номера соседних слайдов. После перенумерации они выправлены под схему 06–48; при правках связок сверяйтесь с актуальным реестром.

---

## 10. Стилевая система

### 10.1 Палитра (`src/styles/global.css`)

```css
--bg:        #FAFAF7    /* основной фон */
--bg-2:      #F2F0EA    /* фон вариант (для секций) */
--ink:       #1A1A1A    /* основной текст */
--ink-soft:  #2B2A28    /* приглушённый текст */
--mute:      #6B6B68    /* серый текст */
--mute-2:    #9A9893    /* светло-серый, чипы, "фантомный" текст */
--rule:      #D9D7CF    /* линии-разделители */
--rule-soft: #ECEAE3    /* мягкие линии */
--accent:    #8B2A1F    /* тёплый бордовый — ТОЛЬКО keystone */
--hi:        #F0EEE8    /* подсветка в схемах, highlight-фон */
```

Алиасы для совместимости: `--text=--ink`, `--text-secondary=--mute`, `--line=--rule`, `--highlight=--hi`.

### 10.2 Палитра типов карточек (`src/routes/Section1.css`)

```css
--type-a-chip: #1F1F1F  / --type-a-tint: #EAE8E2   /* серо-беж */
--type-b-chip: #6B4F0E  / --type-b-tint: #F2EAD2   /* охра/крем */
--type-c-chip: #2C5F5F  / --type-c-tint: #DEE9E5   /* тил/палевый */
--type-d-chip: #5E3F87  / --type-d-tint: #E5DDEE   /* пурпур/лаванда */
--type-e-chip: #8B2A1F  / --type-e-tint: #EFDDDA   /* акцент/розе */
```

Текст на чипе — `#FFFFFF` для всех типов. Карта Секции 2 использует те же переменные.

### 10.3 Шрифты

- **IBM Plex Sans** (400 / 500 / 600) — основной
- **IBM Plex Mono** (400 / 500) — служебный текст, числа, лейблы
- **IBM Plex Serif** (400 italic) — амперсанд в титуле и огромная римская цифра в A-слайдах

Загружаются с Google Fonts в `index.html`.

### 10.4 Tone of voice (контент)

- Без снисходительности и патернализма.
- Без позиционирования через страх.
- Через метафоры, не через жаргон.
- Запретные слова в формулировках: «страх», «бояться», «не страшно», «поручить», «мифы».
- Главные метафоры: Т9 → автозаполнение → GPT (лестница угадывания), LLM как «попугай, проглотивший интернет», ChatGPT как «дрессированный начитанный человек» (RLHF), думающие модели как «студент с черновиком».

---

## 11. Workflow разработки

### Запуск (с нуля на новой машине)

```bash
mise install        # подтягивает Node 25 + pnpm последний по .mise.toml
pnpm install
pnpm dev            # http://localhost:5190/
```

### Создать новый слайд

1. Создать `src/slides/<секция>/SlideXX.jsx`. Скопировать ближайший по типу:
   - **C-визуал** → `Slide07.jsx` (SVG-устройство), `Slide08.jsx` (схема), `Slide10.jsx` / `Slide11.jsx` (UI-mock)
   - **B-тезис** → собрать вручную (центральная фраза + подблок сверху)
   - **D-сравнение** → скопировать `Slide09.jsx` + `Slide09.css`
   - **E-момент** → собрать через `<ESlide variant="momentum|number|live" ... />`
   - **A-дивайдер** → `<ASlide num="..." roman="..." title="..." />`
2. В соответствующем `index.js`:
   - Импортировать компонент.
   - Заменить строку `draft('XX', ...)` на `ready('XX', 'TYPE', 'Title', 'Подблок', SlideXX[, { hasNotes: false }])`.
3. Слайд автоматически появится: по адресу `/slide/XX`, в карте секции и в sidebar (настоящий рендер вместо stub).

### Добавить speaker notes

```bash
cp src/notes/section-1/_template.md src/notes/section-1/slide-XX.md
# отредактировать
```

В реестре убедиться, что нет `{ hasNotes: false }` (по умолчанию true). Vite через `import.meta.glob` подхватит автоматически.

### Печать в PDF

Открыть `/print` (всё) или `/slide/XX` (один) → `Cmd+P`/`Ctrl+P` → Save as PDF → Layout: Landscape → Margins: None → Background graphics: ON → Save. Готовый файл лежит в `public/seminar-ai-llm.pdf` — после изменений в деке его нужно перегенерировать.

### Прод-сборка

```bash
pnpm build      # → dist/
pnpm preview    # локальный smoke
```

---

## 12. Папка `stuff/` — что там и зачем

Все референсы от Claude Design (Claude.ai с design-режимом) и Claude App (chat), на которых построен проект.

### 12.1 Программа и брифы (`stuff/md/`)

- `Программа_семинара_AI_LLM_v3_1.md` — **главный документ**. Структура всего семинара. Версия v3.1.
- `section-1-map.md` — **карта Секции 1**, источник истины для содержания.
- `Section_2_slides_draft.md` — **карта Секции 2**.
- `section_1_slides_list_draft.md` — устаревший черновик С1 с другой нумерацией (НЕ использовать).
- `Brief_for_Claude_Design.md` — бриф для дизайнера.
- `slide-*-notes.md` — заметки ведущего (перенесены в `src/notes/section-1/`).
- `slide-*-task.md` — спецификации для реализации отдельных слайдов.

> Карты в `stuff/md/` написаны под исходную нумерацию С1 (06–58 с E-моментами). Код Секции 1 от них **отошёл**: E-моменты убраны, нумерация сжата в 06–48. При сверке содержания карты годятся, но нумерацию брать из реестра в коде.

### 12.2 HTML-исходники от Claude Design

- `Concept v1.html` — концепт-каталог типов A/B/C/D/E. Дизайн-эталон.
- `Pair 1 - Opening.html` — исходник открытия (5 слайдов). Перенесён в React.
- `Section 1 E-slides.html` — исходные E-слайды Секции 1 (большинство убраны из деки).
- `Section 1 Skeleton.html` — старый черновик; нумерация и подблоки **не совпадают** с актуальной картой — использовать только как источник текстов и идей, не как структуру.
- `Section 1 Slide 07.html`, `08.html`, `10.html` — отдельные финализированные слайды.
- `Section 1 Map.html` — визуальный референс карты секции.

### 12.3 PDF-эталоны (`stuff/pdf/`)

Готовые PDF слайдов от Claude Design — для визуальной сверки рендера в React.

### 12.4 Прочее

- `stuff/img/` — скриншоты.
- `stuff/deck-stage.js`, `tweaks-panel.jsx` — артефакты Claude Design, в React не нужны.

`stuff/` симлинком виден в браузере как `/stuff/...`.

---

## 13. Что готово / что в работе

### Готово

- ✅ Инфраструктура: Vite + React + Router + pnpm + mise
- ✅ Слайд-движок: Stage, useFit, ThumbnailContext, регистр
- ✅ Маршрутизация: главная, открытие, С1, С2, печать, слайд, notes
- ✅ Карты секций С1 (43) и С2 (23), мини-карта открытия (5)
- ✅ Все слайды открытия (01–05) с notes 01–03
- ✅ **Вся Секция 1 (43 слайда, 06–48)** реализована; keystone — слайд 28
- ✅ Speaker notes Секции 1 для всех слайдов, кроме заставки 06
- ✅ **Секция 2 — Части IV и V (23 слайда, 63–87)** реализованы
- ✅ SlideNav (бар + стрелки клавиатуры + Esc на нужную карту)
- ✅ SlideSidebar (миниатюры всех слайдов, авто-скролл к текущему)
- ✅ Markdown viewer для notes со стилизованным prose
- ✅ Печать в PDF (1920×1080, sidebar/nav скрываются) + готовый `public/seminar-ai-llm.pdf`
- ✅ Главная: список готовых слайдов разбит по секциям

### В работе / следующие шаги

- 🟡 **Часть VI Секции 2 (88–102)** — написаны, но скрыты из реестра; решить судьбу и опубликовать/доработать.
- 🟡 **Speaker notes для Секции 2** (сейчас `hasNotes: false` у всех).
- 🟡 Перегенерировать `public/seminar-ai-llm.pdf` после правок деки.
- ⚪ **Секция 3** (90 мин, ~50 слайдов) — программа есть, ни реестра, ни компонентов.

---

## 14. Что НЕ делать в проекте

- AI-сток (роботы, нейросети 3D, синие градиенты)
- Иконки человечков с лампочкой
- Тёмная тема для слайдов (зал дневной, проектор)
- Анимации появления / переходов
- Bullet-list по 5+ пунктов
- Отступления от палитры/типографики
- Изменение `done` на `true` без реальной готовности слайда (карта потеряет наглядность прогресса)
- Использовать `Section 1 Skeleton.html` как структурный источник — нумерация устарела, использовать только как банк текстов
- Использовать `--accent` (#8B2A1F) где-то кроме keystone (слайд 28)
- Использовать фирменные цвета/шрифты сторонних продуктов (Gmail, Google, Яндекс) при рисовании UI-mock — только нейтральные реконструкции в палитре семинара

---

## 15. Удобные ссылки на dev-сервере

| URL | Что |
|---|---|
| http://localhost:5190/ | Главная |
| http://localhost:5190/opening | Карта открытия |
| http://localhost:5190/section/1 | Карта Секции 1 |
| http://localhost:5190/section/2 | Карта Секции 2 |
| http://localhost:5190/print | Все слайды подряд (для PDF) |
| http://localhost:5190/slide/28 | Главный тезис (keystone) |
| http://localhost:5190/slide/10 | Smart Compose в Gmail (C-слайд) |
| http://localhost:5190/notes/20 | Заметки слайда 20 (RLHF) |
| http://localhost:5190/stuff/md/section-1-map.md | Карта С1 (raw, источник содержания) |
| http://localhost:5190/stuff/md/Section_2_slides_draft.md | Карта С2 (raw, источник содержания) |

---

## 16. Контракт для ассистента-помощника

Если вы (Claude.ai) хотите помочь:

- **Добавить/править слайд С1**: текст и тип брать из `stuff/md/section-1-map.md`, но **нумерацию — из реестра в коде** (`src/slides/section-1/index.js`, схема 06–48), карты в `stuff/` отстают.
- **Добавить слайд С2**: текст брать из `stuff/md/Section_2_slides_draft.md`. По C-слайдам с примерами промтов есть унифицированный паттерн «промт → ответ» — переиспользовать.
- **Изменить дизайн** какого-то типа — править соответствующий `?-slides.css` и универсальный компонент. Изменение применится ко всем слайдам этого типа разом.
- **Сменить палитру** — `src/styles/global.css` (общая) или `src/routes/Section1.css` (типы карточек).
- **Не менять**:
  - Сетку 1920×1080 (используется в `@page` для печати).
  - Имя класса `.slide` (на нём держится Stage, печать, миниатюры).
  - Поле `done` в реестре (карта на нём держится).
  - `--accent: #8B2A1F` — это смысловой цвет «главный тезис», его нельзя ставить на проходные слайды.
  - Номера слайдов и перекрёстные ссылки в notes без сверки с реестром (нумерация с пропусками: 06–48, затем 63–87).

---

*Документ обновляется руками. Последнее обновление — после удаления E-моментов из Секции 1 и её перенумерации в 06–48 (keystone → слайд 28); состояние: С1 — 43 слайда, С2 (Части IV–V) — 23 слайда, всего 71.*
